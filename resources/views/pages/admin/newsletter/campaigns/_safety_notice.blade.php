<div class="alert alert-info border-0 d-flex gap-2 align-items-start" role="alert">
  <i class="bi bi-info-circle-fill mt-1"></i>
  <div class="small">
    <strong>{{ __('CMS-side sending is intentionally limited.') }}</strong>
    {{ __('Agares CMS supports campaign drafts, templates, previews and single test emails. Bulk sending to subscribers is disabled in the CMS — it will be delegated to the Agares SaaS API in a future phase. No queue worker, cron, or scheduler setup is required for this phase.') }}
  </div>
</div>
